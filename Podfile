# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

target 'Meetup-CleanSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Meetup-CleanSwift
  pod 'Alamofire'

  target 'Meetup-CleanSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Meetup-CleanSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
